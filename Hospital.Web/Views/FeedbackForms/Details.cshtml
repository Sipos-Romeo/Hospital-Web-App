@using Hospital.Services
@model Hospital.Models.FeedbackForm

@{
    ViewData["Title"] = "Feedback Form Details";
}

<div class="container mt-5">
    <!-- Page Header -->
    <div class="row align-items-center mb-4">
        <div class="col-md-8">
            <h1 class="display-6">Feedback Form Details</h1>
            <p class="text-muted">View the details of the selected feedback form.</p>
        </div>
        <div class="col-md-4 text-md-right text-center">
            <a class="btn btn-outline-secondary" asp-action="Index">
                <i class="fa fa-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    <!-- Details Card -->
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h4 class="card-title text-primary text-center mb-4">Feedback Information</h4>
                    <hr />

                    <!-- Feedback Details -->
                    <dl class="row mb-4">
                        <dt class="col-sm-4">
                            <i class="fa fa-user text-primary mr-2"></i>
                            Name
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Name)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-info-circle text-primary mr-2"></i>
                            Description
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Description)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-envelope text-primary mr-2"></i>
                            Email
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Email)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-phone text-primary mr-2"></i>
                            Phone
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Phone)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-star text-primary mr-2"></i>
                            Rating Section
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.RatingSection)</dd>
                    </dl>

                    <hr />

                    <!-- Action Buttons -->
                    <div class="d-flex justify-content-between">
                        <a class="btn btn-outline-secondary" asp-action="Index">
                            <i class="fa fa-arrow-left"></i> Back to List
                        </a>
                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.Id">
                            <i class="fa fa-edit"></i> Edit
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
