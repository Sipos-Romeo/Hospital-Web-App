@using Hospital.Services
@model Hospital.Models.HospitalInfo

@{
    ViewData["Title"] = "Hospital Details";
}

<div class="container mt-5">
    <!-- Page Header -->
    <div class="row align-items-center mb-4">
        <div class="col-md-8">
            <h1 class="display-6">Hospital Details</h1>
            <p class="text-muted">View the details of the selected hospital entry.</p>
        </div>
        <div class="col-md-4 text-md-right text-center">
            <a class="btn btn-outline-secondary" asp-action="Index">
                <i class="fa fa-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    <!-- Details Card -->
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h4 class="card-title text-primary mb-4 text-center">@Model.Name</h4>
                    <hr />

                    <!-- Details List -->
                    <dl class="row">
                        <dt class="col-sm-4">
                            <i class="fa fa-hospital text-primary mr-2"></i>
                            Name
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Name)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-tags text-primary mr-2"></i>
                            Type
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Type)</dd>

                        <dt class="col-sm-4">
                            <i class="fa fa-city text-primary mr-2"></i>
                            City
                        </dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.City)</dd>
                    </dl>

                    <hr />

                    <!-- Action Buttons -->
                    <div class="d-flex justify-content-between">
                        <a class="btn btn-outline-secondary" asp-action="Index">
                            <i class="fa fa-arrow-left"></i> Back to List
                        </a>
                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id">
                            <i class="fa fa-edit"></i> Edit
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
